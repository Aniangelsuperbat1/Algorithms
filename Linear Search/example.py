# Linear search: Searches through a set of data to see if target element is in data set.

# O(N) time

Data: (1, 3, 5, 67, 12, 54, 75, 342)
Target: 54


def Linear_Search(Data, target):
    for num in range(len(data)):
        if Data[num] == (Target):
            Return True
    Return False
